APP_PORT=8080
APP_ENV=development
APP_BASE_URL=http://localhost:8080
AUTO_MIGRATE=true
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

DATABASE_URL=postgres://admin:localpass@postgres:5432/koydensehire?sslmode=disable
DATABASE_MAX_CONNECTIONS=25
DATABASE_MAX_IDLE=5

REDIS_URL=redis://redis:6379
REDIS_PASSWORD=

JWT_SECRET=your-super-secret-key-change-in-production-min-32-chars
JWT_ACCESS_TOKEN_EXPIRY=24h

OTP_EXPIRY_SECONDS=300
OTP_MAX_ATTEMPTS=3
OTP_RESEND_COOLDOWN_SECONDS=60

STORAGE_PROVIDER=r2
S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
S3_BUCKET=koydensehire
S3_ACCESS_KEY=your-r2-access-key
S3_SECRET_KEY=your-r2-secret-key
S3_PUBLIC_URL=https://files.koydensehire.com

NETGSM_USERNAME=your-netgsm-username
NETGSM_PASSWORD=your-netgsm-password
NETGSM_HEADER=KOYDENSEHIRE

N8N_WEBHOOK_URL=http://n8n:5678/webhook
N8N_WEBHOOK_SECRET=your-webhook-secret

CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token

# Deep link domain doğrulama (App Links / Universal Links)
# Boş bırakılırsa /.well-known endpoint'leri 404 döner (güvenli varsayılan).
# Fingerprint almak için (debug keystore):
#   keytool -list -v -keystore %USERPROFILE%\.android\debug.keystore -alias androiddebugkey -storepass android | findstr SHA256
# Birden fazla fingerprint virgülle ayrılır (debug + Play App Signing).
ANDROID_PACKAGE_NAME=com.koydensehire.koyden_sehire
ANDROID_SHA256_CERT_FINGERPRINTS=
# Apple: "<TeamID>.<BundleID>" formatında, örn. ABCDE12345.com.koydensehire.koydenSehire
APPLE_APP_ID=
